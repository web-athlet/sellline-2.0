{
  "name": "@nextgen/integration-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest --run",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
